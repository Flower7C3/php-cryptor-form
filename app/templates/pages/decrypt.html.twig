{% extends '@app/pages/form.html.twig' %}
{% import "@app/meta/forms.html.twig" as forms %}

{% block secret1_form_group_class %}col-md-10{% endblock %}
{% block secret2_form_group %}{% endblock %}
{% block hash_form_group_class %}col-md-2{% endblock %}
{% block input_form_group %}
    <div class="form-group">
        <label for="secret">
            <em class="far fa-fw fa-comment-alt"></em>
            Encrypted message to decode
        </label>
        <div class="input-group">
            <input type="password" name="encrypted" class="form-field-encrypted form-control form-control-lg{% if form.errors.encrypted %} is-invalid{% endif %} js-tooltip" value="{{ form.data.encrypted }}" placeholder="Encrypted message to decode" data-toggle="tooltip" data-placement="top" title="Encrypted message to decode" {% if form.autofocus === 'encrypted' %} autofocus{% endif %}/>
            <div class="input-group-append">
                <div class="btn btn-outline-warning btn-lg js-tooltip js-input-password-toggle" role="button" for=".form-field-encrypted" data-toggle="button" data-toggle="tooltip" data-placement="top" title="Show encrypted message">
                    <em class="fas fa-fw fa-eye"></em>
                </div>
            </div>
            {{ forms.error(form, 'encrypted') }}
        </div>
    </div>
{% endblock %}
